<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FragmentTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FragmentTask" parent="Base.Theme.FragmentTask" />

    <style name="MyButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="shapeAppearanceOverlay">@style/MyButtonShape</item> <!-- Используем форму -->
        <item name="android:textColor">#000000</item>
        <item name="android:backgroundTint">#808080</item>
    </style>

    <!-- Определяем форму с закругленными углами -->
    <style name="MyButtonShape" parent="ShapeAppearance.MaterialComponents.SmallComponent">
        <item name="cornerSize">16dp</item> <!-- Закругленные углы -->
    </style>
</resources>